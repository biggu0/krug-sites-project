# JHT 图片处理系统使用说明

## 1. 登录系统

使用管理员分配的用户名和密码登录。不同账号的本地批次存档相互独立。

## 2. 准备模板

进入左侧“模板管理”：

1. 导入 PDF 模板。
2. 设置页数、有无封面、单双面印刷、插图页面和图片旋转参数。
3. 绘制定制区域，或导入 SVG 轮廓后调整位置和大小。
4. 如有前景保护内容，导入对应的前景文件。
5. 点击保存模板配置。

模板名称需要与订单表格中的 SKU货号一致。自定义模板与内置模板同名时，优先使用自定义模板。

## 3. 新建批次

在订单列表上方点击“新建批次”。系统会按日期自动命名，例如：

`2026-08-27 第1批`

当天继续新建时会自动变成第2批、第3批。低频修改名称时，可点击“重命名”。

## 4. 导入订单

点击“导入订单文件夹”，选择包含多个订单子文件夹的总文件夹。

- 每个子文件夹代表一个订单。
- 有效图片名称应包含“消费者上传原图+序号”。
- 重复导入同名订单时会自动跳过，不会覆盖已有调整。
- 根目录中如有 Excel 订单表格，系统会自动读取。
- 也可以单独点击“导入订单表格”。

Excel 需要包含：定制ID、SKU货号、收货仓库、发货数量。

## 5. 订单和模板状态

- `表格：未匹配`：订单文件夹名称没有匹配到 Excel 中的定制ID。
- `模板：待匹配`：由于没有取得 SKU，暂时无法判断模板。
- `模板：未匹配`：已经取得 SKU，但系统中没有对应模板。
- 发货数量显示 `—`：该订单没有匹配到表格，无法取得发货数量。
- `待排版`：订单仍需检查或调整。
- `已排版`：订单已经确认，可以参与批量导出。

## 6. 调整图片

点击页面选择图片，双击可进入放大调整。

- 鼠标拖动：移动图片。
- 鼠标滚轮：缩放图片，范围为 0.5–5。
- Option/Alt + 滚轮：精细旋转。
- 方向键：移动图片。
- `+` / `-`：缩放图片。
- `R`：顺时针旋转5度。
- `Shift + R`：逆时针旋转5度。
- `PageUp` / `PageDown`：切换上一张、下一张。
- `0`：重置当前图片。

蓝色线框表示图片边界，拖动四角可等比例缩放。移动到定制区域中心或边缘附近时会自动吸附，并显示参考线。

## 7. 确认订单

检查全部页面后点击“确认已排版”。

- 鼠标点击确认：只确认当前订单。
- 按一次回车：确认当前订单。
- 再按一次回车：进入下一个待排版订单。
- 点击“下一个”：直接进入下一个待排版订单。

没有匹配模板的订单在确认时会要求再次确认。

## 8. 保存批次

订单调整完成后点击“保存批次”。只有点击保存后，当前批次才会写入浏览器存档。

- 已保存批次可以在下拉列表中重新打开并继续修改。
- 修改后显示“未保存”，需要再次点击“保存批次”。
- 新建或切换批次时，如有未保存内容，系统会提示确认。
- “批次管理”支持勾选、全选和批量删除存档。

批次数据保存在当前电脑的当前浏览器中，不会自动同步到其他电脑。清理浏览器数据可能导致存档丢失。

## 9. 导出文件

- “导出当前”：导出当前订单。
- “PDF导出到文件夹”：选择文件夹后批量保存 PDF。
- “ZIP批量导出”：将已排版订单打包下载。

如果发货数量大于1，系统会按数量生成多份文件，并在文件名末尾添加序号。

## 10. 操作注意事项

- 导出前确认模板、页数、定制区域和前景保护层正确。
- 导入订单后先检查表格和模板匹配状态。
- 重要操作完成后及时点击“保存批次”。
- 不要在未保存时刷新网页、清理浏览器数据或更换浏览器。
- 删除订单或批次前认真核对名称；删除存档后无法恢复。
